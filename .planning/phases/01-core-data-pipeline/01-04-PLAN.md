---
phase: 01-core-data-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/api/cron/seed-birdeye/route.ts
  - vercel.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Token registry can be seeded from Birdeye rankings API"
    - "Seeding runs daily to refresh token list"
    - "Top tokens by market cap are tracked per chain"
  artifacts:
    - path: "src/app/api/cron/seed-birdeye/route.ts"
      provides: "Token registry seeding cron job"
      exports: ["POST", "GET"]
    - path: "vercel.json"
      provides: "Cron schedule for daily seeding"
      contains: "seed-birdeye"
  key_links:
    - from: "src/app/api/cron/seed-birdeye/route.ts"
      to: "src/lib/db/birdeye-queries.ts"
      via: "seedTokensFromBirdeye function"
      pattern: "seedTokensFromBirdeye"
---

<objective>
Create the token registry seeding cron job that populates the birdeye_tokens table from Birdeye rankings.

Purpose: Maintain an up-to-date token registry so the price polling cron has tokens to fetch.
Output: Working seed endpoint that populates tokens for all 11 chains.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-data-pipeline/01-RESEARCH.md
@.planning/phases/01-core-data-pipeline/01-01-SUMMARY.md
@.planning/phases/01-core-data-pipeline/01-02-SUMMARY.md

# Reference existing patterns
@src/app/api/cron/collect-prices/route.ts
@src/lib/qstash/verify.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed-birdeye cron endpoint</name>
  <files>src/app/api/cron/seed-birdeye/route.ts</files>
  <action>
Create new file `src/app/api/cron/seed-birdeye/route.ts`:

```typescript
// Cron: Seed Birdeye token registry
// Schedule: Daily at 4 AM UTC
// Purpose: Populate/refresh token registry from Birdeye rankings

import { NextRequest, NextResponse } from 'next/server';
import { verifyCronRequestWithDevBypass } from '@/lib/qstash/verify';
import { BIRDEYE_CHAINS, BirdeyeChain } from '@/lib/apis/birdeye';
import { seedTokensFromBirdeye, getTokenCountByChain } from '@/lib/db/birdeye-queries';
import { checkRateLimit, RATE_LIMITS } from '@/lib/rate-limiter/distributed';

// Route config for extended timeout
export const maxDuration = 60;

// Tokens to seed per chain (based on CONTEXT.md: 100-200 per chain)
const TOKENS_PER_CHAIN = 150;

// Chains in priority order
const CHAIN_PRIORITY: BirdeyeChain[] = [
  'solana',
  'ethereum',
  'base',
  'arbitrum',
  'bsc',
  'polygon',
  'optimism',
  'avalanche',
  'zksync',
  'sui',
  'aptos',
];

async function handler(req: NextRequest): Promise<NextResponse> {
  const startTime = Date.now();
  console.log('[seed-birdeye] Starting token registry seeding...');

  const results: Record<string, { added: number; updated: number; error?: string }> = {};
  let totalAdded = 0;
  let totalUpdated = 0;

  try {
    // Get initial token counts
    const initialCounts = await getTokenCountByChain();
    console.log('[seed-birdeye] Initial token counts:', initialCounts);

    // Seed each chain sequentially
    for (const chain of CHAIN_PRIORITY) {
      // Check rate limit before each chain
      const rateCheck = await checkRateLimit(RATE_LIMITS.BIRDEYE_ACCOUNT);
      if (!rateCheck.allowed) {
        console.log(`[seed-birdeye] Rate limit hit at chain ${chain}, waiting...`);
        // Wait for reset and continue (seeding is less time-sensitive)
        const waitTime = rateCheck.resetAt - Date.now();
        if (waitTime > 0 && waitTime < 65000) { // Max wait 65s to stay under timeout
          await new Promise(resolve => setTimeout(resolve, waitTime));
        } else {
          results[chain] = { added: 0, updated: 0, error: 'Rate limit, skipped' };
          continue;
        }
      }

      try {
        console.log(`[seed-birdeye] Seeding ${chain}...`);
        const result = await seedTokensFromBirdeye(chain, TOKENS_PER_CHAIN);
        results[chain] = result;
        totalAdded += result.added;
        totalUpdated += result.updated;
        console.log(`[seed-birdeye] ${chain}: added=${result.added}, updated=${result.updated}`);

        // Delay between chains to spread rate limit usage
        await new Promise(resolve => setTimeout(resolve, 200));

      } catch (error) {
        const errorMessage = error instanceof Error ? error.message : 'Unknown error';
        console.error(`[seed-birdeye] Error seeding ${chain}:`, error);
        results[chain] = { added: 0, updated: 0, error: errorMessage };
      }
    }

    // Get final token counts
    const finalCounts = await getTokenCountByChain();

    const duration = Date.now() - startTime;
    console.log(`[seed-birdeye] Completed in ${duration}ms: added=${totalAdded}, updated=${totalUpdated}`);

    return NextResponse.json({
      success: true,
      totalAdded,
      totalUpdated,
      results,
      tokenCounts: {
        before: initialCounts,
        after: finalCounts,
      },
      durationMs: duration,
      timestamp: new Date().toISOString(),
    });

  } catch (error) {
    console.error('[seed-birdeye] Error:', error);

    return NextResponse.json({
      success: false,
      error: error instanceof Error ? error.message : 'Unknown error',
      results,
      timestamp: new Date().toISOString(),
    }, { status: 500 });
  }
}

export const POST = verifyCronRequestWithDevBypass(handler);

// Allow GET for local testing and manual trigger
export async function GET(req: NextRequest) {
  if (process.env.NODE_ENV === 'production') {
    return NextResponse.json({ error: 'Method not allowed' }, { status: 405 });
  }
  return handler(req);
}
```

Key design decisions:
- 150 tokens per chain (middle of 100-200 range from CONTEXT.md)
- Can wait for rate limit reset since seeding is less time-sensitive
- Sequential processing to manage rate limits
- Reports before/after token counts for visibility
  </action>
  <verify>
TypeScript compiles: `cd /Users/cecvic/Cec-Projects/Kai\ Projects/crypto-ranking && npx tsc --noEmit`
  </verify>
  <done>
- seed-birdeye/route.ts exists with POST and GET handlers
- Seeds 150 tokens per chain from Birdeye rankings
- Handles rate limiting with optional wait
- Returns before/after token counts
  </done>
</task>

<task type="auto">
  <name>Task 2: Add seed cron schedule to vercel.json</name>
  <files>vercel.json</files>
  <action>
Update `vercel.json` to add the seed-birdeye cron schedule:

Add to the `crons` array:
```json
{
  "path": "/api/cron/seed-birdeye",
  "schedule": "0 4 * * *"
}
```

This runs daily at 4 AM UTC, after the cleanup job (3 AM) and before the main traffic hours.

The final crons array should have 3 entries: cleanup, poll-birdeye, seed-birdeye.
  </action>
  <verify>
Validate JSON syntax: `cd /Users/cecvic/Cec-Projects/Kai\ Projects/crypto-ranking && node -e "JSON.parse(require('fs').readFileSync('vercel.json'))" && echo "JSON valid"`
  </verify>
  <done>
- vercel.json contains seed-birdeye cron entry
- Schedule is daily at 4 AM UTC (0 4 * * *)
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. GET /api/cron/seed-birdeye returns response in development
3. vercel.json is valid JSON with all 3 cron entries
</verification>

<success_criteria>
- Seed cron job can populate token registry for all 11 chains
- Rate limiting prevents quota exhaustion
- Token counts reported for monitoring
- Vercel cron scheduled for daily execution
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-data-pipeline/01-04-SUMMARY.md`
</output>
