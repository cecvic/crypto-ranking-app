---
phase: 01-core-data-pipeline
plan: 08
type: execute
wave: 6
depends_on: ["01-07"]
files_modified:
  - src/app/(dashboard)/tokens/page.tsx
  - src/components/navigation/sidebar.tsx (or equivalent)
autonomous: false

must_haves:
  truths:
    - "User can navigate to /tokens page from dashboard"
    - "Tokens page displays BirdeyeTokenTable"
    - "Page is accessible only to authenticated users"
  artifacts:
    - path: "src/app/(dashboard)/tokens/page.tsx"
      provides: "Tokens page with BirdeyeTokenTable"
      min_lines: 20
  key_links:
    - from: "src/app/(dashboard)/tokens/page.tsx"
      to: "src/components/tokens/birdeye-token-table.tsx"
      via: "BirdeyeTokenTable component"
      pattern: "BirdeyeTokenTable"
---

<objective>
Create the tokens page and add navigation to it, then verify the complete data pipeline works.

Purpose: Connect all the pieces together into a user-facing feature.
Output: Working /tokens page accessible from dashboard navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-data-pipeline/01-07-SUMMARY.md

# Reference existing page patterns
@src/app/(dashboard)/page.tsx
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tokens page</name>
  <files>src/app/(dashboard)/tokens/page.tsx</files>
  <action>
Create new file `src/app/(dashboard)/tokens/page.tsx`:

```typescript
import { Metadata } from 'next';
import { BirdeyeTokenTable } from '@/components/tokens/birdeye-token-table';

export const metadata: Metadata = {
  title: 'Token Rankings | Crypto Rankings',
  description: 'Multi-chain token rankings from Birdeye across 11 blockchains',
};

export default function TokensPage() {
  return (
    <div className="container py-6 space-y-6">
      <div>
        <h1 className="text-2xl font-bold tracking-tight">Token Rankings</h1>
        <p className="text-muted-foreground">
          Real-time token prices and metrics across 11 blockchains, powered by Birdeye.
        </p>
      </div>

      <BirdeyeTokenTable limit={100} />
    </div>
  );
}
```

Note: This page is within the (dashboard) route group, which should already have authentication middleware. If not, auth is handled by the API routes.
  </action>
  <verify>
TypeScript compiles: `cd /Users/cecvic/Cec-Projects/Kai\ Projects/crypto-ranking && npx tsc --noEmit`
  </verify>
  <done>
- tokens/page.tsx exists in dashboard route group
- Page renders BirdeyeTokenTable
- Metadata includes title and description
  </done>
</task>

<task type="auto">
  <name>Task 2: Add navigation link to tokens page</name>
  <files>src/components/navigation/sidebar.tsx (or equivalent navigation component)</files>
  <action>
Find and update the main navigation component (likely sidebar.tsx, nav.tsx, or similar).

Add a navigation link for the tokens page:

```typescript
// Add to navigation items array
{
  href: '/tokens',
  label: 'Tokens',
  icon: CoinsIcon, // or similar icon from lucide-react
}
```

If the navigation uses a different pattern, follow the existing convention.

First, search for navigation files:
- Check `src/components/navigation/` directory
- Check `src/components/layout/` directory
- Check the dashboard layout file for inline navigation

Add the tokens link following the existing pattern.
  </action>
  <verify>
TypeScript compiles and navigation file is updated. The exact file depends on the codebase structure.
  </verify>
  <done>
- Navigation includes link to /tokens
- Link is visible in the dashboard sidebar/nav
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Birdeye data pipeline with:
- API client for all 11 chains
- Database schema and queries
- Price polling cron (every 15 min)
- Token seeding cron (daily)
- API endpoints (/api/tokens, /api/tokens/[chain])
- React hook (useBirdeyeTokens)
- UI components (BirdeyeTokenTable, ChainSelector)
- Tokens page with navigation
  </what-built>
  <how-to-verify>
1. Start dev server: `pnpm dev`
2. Navigate to http://localhost:3000/tokens (you may need to sign in first)
3. Verify:
   - Token table loads (may be empty initially if no data seeded)
   - Chain selector dropdown works
   - Selecting a chain filters the table
   - Loading states display while fetching
   - Error state displays if API fails (can test by stopping server briefly)

4. Test seeding (in a separate terminal):
   - `curl -X GET http://localhost:3000/api/cron/seed-birdeye` (GET allowed in dev)
   - Check response shows tokens added/updated per chain

5. Test price polling:
   - `curl -X GET http://localhost:3000/api/cron/poll-birdeye`
   - Check response shows chains polled and tokens updated

6. Refresh /tokens page - data should now appear

7. Try different chains in selector - verify data changes appropriately
  </how-to-verify>
  <resume-signal>Type "approved" if all verification steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. /tokens page accessible from dashboard
3. BirdeyeTokenTable renders with chain filtering
4. Cron endpoints return data in development
5. End-to-end flow: seed -> poll -> display works
</verification>

<success_criteria>
- User can navigate to /tokens from dashboard
- Token table displays Birdeye data across chains
- Chain selector filters tokens by blockchain
- Data refreshes via cron jobs
- Cache provides fast subsequent loads
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-data-pipeline/01-08-SUMMARY.md`
</output>
